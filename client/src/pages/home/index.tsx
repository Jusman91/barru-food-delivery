import { HeroHome } from '@/components';
import ServiceBanner from '@/components/ServiceBanner';

const Home = () => {
	return (
		<section className=''>
			<HeroHome />
			<ServiceBanner />
		</section>
	);
};

export default Home;
